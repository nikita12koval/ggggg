#include <iostream>
using namespace std;
int main()
{
    using namespace std;
    int s, l1, r1, l2, r2;
    int x1=1, x2=2;

    cin >> s >> l1 >> r1 >> l2 >> r2;
    if (pow(-10, 15) <= s, l1, r1, l2, r2 <= pow(10, 15), l1 <= r1, l2 <= r2)
    {
        cout << x1 << x2;
    }
    else
    {
        cout << -1;
    }
    
   
    
    
        
}